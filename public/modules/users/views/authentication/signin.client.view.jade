.login-form(data-ng-controller='AuthenticationController')
    form(name="signinForm", data-ng-submit="signin(signinForm.$valid)", novalidate, autocomplete="off")
        .form-group
            a.btn.btn-primary.form-control(href='/auth/facebook') Log in with Facebook
        .login-label
            | ------------------------ OR ------------------------ 
            br
            br
            |           Log in with your username
        .form-group
            input.form-control(type='text', placeholder='Username', ng-model='user.username')
        .form-group
            input.form-control(type='password', placeholder='Password', ng-model='user.password')
        .form-group.text-right
            button.btn.btn-default(type='submit') Log in